# 档位（LadderProfile）逻辑说明

## 一、档位定义

```swift
enum LadderProfile: Int, CaseIterable {
    case low       // 超低网
    case standard  // 高清
    case high      // 超清
    case ultra     // 超高帧
    case p4k       // 超高清
}
```

## 二、档位参数总览

### 后置摄像头

| 档位 | UI名称 | 输出分辨率 | 采集分辨率 | 采集FPS | 最大码率 | scaleDown | 说明 |
|------|--------|-----------|-----------|---------|---------|-----------|------|
| p4k | 超高清 | 1920x1080 (iPhone15+) | 1920x1080 | 60 | 5500kbps | 1.0 | iPhone15+ 直接采集16:9 |
| p4k | 超高清 | 1920x1440 (iPhone14-) | 1920x1440 | 60 | 5500kbps | 1.0 | iPhone14及以下 原始4:3 |
| ultra | 超高帧 | 1280x720 | 1280x720 | 240 | 3500kbps | 1.0 | 16:9 单独采集 |
| high | 超清 | 1440x1080 | 1920x1440 | 60 | 3500kbps | 4/3≈1.33 | 4:3 等比缩放 |
| standard | 高清 | 768x576 | 1920x1440 | 60 | 2500kbps | 2.5 | 4:3 等比缩放 |
| low | 超低网 | 640x480 | 1920x1440 | 60 | 2000kbps | 3.0 | 4:3 等比缩放 |

### 前置摄像头

与后置相同，唯一区别：**ultra 采集FPS = 120**（后置240）

## 三、采集分辨率分组

系统中存在 **三种采集分辨率**，档位切换时采集分辨率变化就需要 **重采集**（recapture）：

```
┌─────────────────────────────────────────────────────────────┐
│ 采集组A: 1920x1440 (4:3)                                    │
│   → low, standard, high                                     │
│   → p4k（仅 iPhone 14 及以下）                                │
│   通过 scaleResolutionDownBy 等比缩放到不同输出分辨率          │
├─────────────────────────────────────────────────────────────┤
│ 采集组B: 1920x1080 (16:9)                                    │
│   → p4k（仅 iPhone 15+）                                     │
│   scaleDown=1.0，直接输出 1920x1080                           │
├─────────────────────────────────────────────────────────────┤
│ 采集组C: 1280x720 (16:9)                                     │
│   → ultra                                                    │
│   scaleDown=1.0，直接输出 1280x720                            │
└─────────────────────────────────────────────────────────────┘
```

### 为什么 p4k 在 iPhone 15+ 需要独立采集？

1. `scaleResolutionDownBy` 是 WebRTC 标准参数，**宽高等比缩放**，无法单独缩一个维度
2. 从 1920x1440 用 scaleDown=4/3 只能得到 1440x1080，不是 1920x1080
3. 要得到 1920x1080 只能直接采集 16:9 格式

### 为什么 iPhone 15+ 不用 1920x1440？

iPhone 15 及以上机型的系统限制，1920x1440 格式不能可靠支持 60fps。iPhone 13/14 没有此问题。

## 四、档位切换逻辑

### 核心函数：`applyProfileBitrateOnly(_ p: LadderProfile)`

```
档位切换请求
    │
    ├── 1. 获取新档位的 LadderPreset
    ├── 2. 更新 currentProfile
    ├── 3. 设置码率（effectiveMaxKbpsForCurrentProfile）
    ├── 4. 更新 FrameThrottler 参数
    │
    ├── 5. 判断是否需要重采集
    │     比较 getCaptureResolutionForProfile(old) vs getCaptureResolutionForProfile(new)
    │     │
    │     ├── 采集分辨率不同 → recaptureWithResolution（停止采集→找新格式→重新启动）
    │     │   例：standard→ultra, high→p4k(15+), ultra→standard ...
    │     │
    │     └── 采集分辨率相同 → setResolutionScale（只改编码器缩放比例，不断流）
    │         例：standard→high, high→low, standard→low ...
    │
    ├── 6. 恢复变焦等相机配置（300ms 延迟）
    └── 7. 发送关键帧（100ms + 200ms 双重保险，防绿幕）
```

### 切换矩阵（iPhone 15+）

从\到 | low | standard | high | p4k | ultra
-----|-----|----------|------|-----|------
low | - | scale | scale | **recapture** | **recapture**
standard | scale | - | scale | **recapture** | **recapture**
high | scale | scale | - | **recapture** | **recapture**
p4k | **recapture** | **recapture** | **recapture** | - | **recapture**
ultra | **recapture** | **recapture** | **recapture** | **recapture** | -

> iPhone 14 及以下：p4k 与 low/standard/high 同组（均为1920x1440），互相切换只需 scale

### 切换矩阵（iPhone 14 及以下）

从\到 | low | standard | high | p4k | ultra
-----|-----|----------|------|-----|------
low | - | scale | scale | scale | **recapture**
standard | scale | - | scale | scale | **recapture**
high | scale | scale | - | scale | **recapture**
p4k | scale | scale | scale | - | **recapture**
ultra | **recapture** | **recapture** | **recapture** | **recapture** | -

## 五、scaleResolutionDownBy 机制

```
采集分辨率 ÷ scaleDown = 输出分辨率（宽高等比）

示例：
  1920x1440 ÷ 1.0   = 1920x1440  (p4k iPhone14-)
  1920x1440 ÷ 1.333 = 1440x1080  (high)
  1920x1440 ÷ 2.5   = 768x576    (standard)
  1920x1440 ÷ 3.0   = 640x480    (low)
  1920x1080 ÷ 1.0   = 1920x1080  (p4k iPhone15+)
  1280x720  ÷ 1.0   = 1280x720   (ultra)
```

**限制**：`scaleResolutionDownBy` 只能等比缩放，不能裁切、不能单独缩宽或高。

## 六、关键函数索引

| 函数 | 位置 | 作用 |
|------|------|------|
| `calculateLadderForDevice` | WebRTCManager.swift | 根据摄像头初始化5档 LadderPreset 配置 |
| `getCaptureResolutionForProfile` | WebRTCManager.swift | 根据档位返回实际采集分辨率（区分 ultra/p4k(15+)/其他） |
| `applyProfileBitrateOnly` | WebRTCManager.swift | 档位切换入口：判断是否 recapture，设码率/缩放/FPS |
| `setResolutionScale` | WebRTCManager.swift | 热切换缩放比例（不重采集，只改编码器 scaleDown） |
| `recaptureWithResolution` | WebRTCManager.swift | 冷切换：停止采集→查找新格式→重新启动采集 |
| `findBestFormat` | WebRTCManager.swift | 查找最佳匹配格式（优先最高FPS） |
| `startCaptureWithDevice` | WebRTCManager.swift | 初始启动采集（统一使用 findBestFormat） |
| `isIPhone15OrNewer` | WebRTCManager.swift | 判断是否 iPhone 15 及以上（影响 p4k 采集策略） |
| `effectiveMaxKbpsForCurrentProfile` | WebRTCManager.swift | 根据档位和质量百分比计算目标码率 |

## 七、FPS 机制

- 后端下发的 FPS 值需要 **÷4** 才是实际推送FPS（如 240→60fps）
- `FrameThrottler` 负责帧率节流：采集FPS 可能远高于推送FPS
- `maxPushFps` 限制每个档位的最高推送FPS（目前全部为 60）
- ultra 后置采集 240fps 但最多推送 60fps；前置采集 120fps

## 八、码率机制

- 每个档位有 `maxKbps` 上限
- 实际码率 = `maxKbps × qualityPercent`（质量百分比由后端/手动控制）
- 码率与 FPS 完全解耦，互不影响
- 使用 CBR（恒定码率），min=max 防止花屏
- 每3秒强制重设码率，对抗 WebRTC 内部自动调整
